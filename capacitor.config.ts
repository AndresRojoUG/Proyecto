import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hvac.app',
  appName: 'HVAC SYSTEMS SUPPORT',
  webDir: 'dist/crud',
  bundledWebRuntime: false
 
};

export default config;
